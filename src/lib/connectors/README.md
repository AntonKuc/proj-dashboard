# Коннекторы внешних сервисов

Каждый файл в этой папке — точка подключения одного внешнего сервиса.
1С, Google Диск — пока заглушки: бросают понятную ошибку, если нужные
ключи не заданы в переменных окружения. Как только дашь доступы —
допишем реальный вызов API внутри `fetch...()` и данные, которые он
вернёт, будем писать в таблицу `Metric` с соответствующим `source`.

## Такском Касса (`taxcom.ts`) — реализован (2026-09)
Нужно: `TAXCOM_INTEGRATOR_ID`, `TAXCOM_LOGIN`, `TAXCOM_PASSWORD`.
Даёт: чистую выручку (за минусом возвратов) по точкам, статья "Выручка".

Важно для этого коннектора конкретно:
- `POST /API/v2/kktstat/shifts` — ожидаемый "простой" эндпоинт для этого —
  **не работает** на этом аккаунте (проверено исчерпывающе, все
  комбинации фильтров). Рабочая цепочка: `Login` (с нужным
  `AgreementNumber`) → `ShiftList` → `ShiftInfo` на каждую смену.
- Один логин (`kucii@inbox.ru`) имеет доступ к 9 личным кабинетам —
  выбирается полем `AgreementNumber` при логине, не по умолчанию.
- Сопоставление код проекта ↔ (agreementNumber, fn) — захардкожено в
  `TAXCOM_LOCATIONS` внутри файла, вручную проверено по всем 11 точкам.
  Полная выкладка (как проверяли, откуда взялись ID) — в проекте
  claude.ai "Работа", файл `claude/taxcom-outlet-code-mapping.md`.
- Ручной/исторический синк: `POST /api/v1/sync/taxcom` с
  `{from, to, projectCodes?}` — максимум 45 дней за раз (иначе таймаут
  serverless-функции), для бэкфилла звать по частям (например, по месяцу).
- Ночной автосинк: `GET /api/v1/sync/taxcom`, по расписанию из
  `vercel.json`, защищён `CRON_SECRET`.

## 1С (`onec.ts`)
Нужно: адрес 1С:Предприятие с включённым HTTP-сервисом (или обмен через
1C:Fresh/облако), логин/пароль или сервисный аккаунт.
Даёт: себестоимость, ФОТ, обязательные платежи, налоги — управленческие
проводки, которые сейчас руками сведены в файле.

## Google Диск (`gdrive.ts`)
Нужно: OAuth-клиент (Client ID/Secret) или service account с доступом
к нужным папкам/таблицам на Google Диске.
Даёт: исходные файлы отчётности (например, обновлённый xlsx), которые
можно парсить автоматически вместо ручной загрузки.

## Яндекс Карты (`yandexMaps.ts`) — реализован (2026-09)
Нужно: **ничего** — ни API-ключа, ни логина. Не нужен `YANDEX_MAPS_API_KEY`
из `.env.example` (оставлен закомментированным на случай, если Яндекс
однажды закроет публичный доступ и придётся возвращаться к API/VPS-схеме).
Даёт: рейтинг, кол-во оценок/отзывов, адрес, координаты и **весь архив
отзывов** (не только последние — так решил Антон 2026-09-10) — для
карточки проекта, не для P&L, поэтому отдельные таблицы `YandexMapsInfo`/
`YandexReview`, а не строка в `Metric`.

Важно для этого коннектора конкретно:
- Всё нужное лежит прямо в HTML страницы организации
  (`https://yandex.com/maps/org/<любой-slug>/<orgId>/`) как микроразметка
  schema.org (`LocalBusiness`/`AggregateRating`/`Review`) — обычный
  `fetch()` без куков и без выполнения JS. Проверено вручную на всех 10
  точках, ни логин, ни API-ключ не потребовались.
- **orgId, а не slug — стабильный ключ** (см. принцип ниже): slug в URL
  чисто косметика, Яндекс резолвит только по числовому orgId (проверено:
  мусорный slug с реальным orgId всё равно открывает нужную организацию).
- Полный архив отзывов — отдельный путь `.../reviews/?page=N`, до ~50 на
  страницу; страница за пределами реального количества отдаёт HTTP 200 с
  пустым списком (не 404) — это и есть сигнал "последняя страница".
- Яндекс не даёт явный ID отзыва в разметке → dedup-ключ —
  `(orgId, authorUserId, publishedAt)`, где `authorUserId` — 3-уровневый
  fallback: ссылка на профиль → URL аватарки → `имя автора` (в этом
  порядке убывающей надёжности; см. комментарии в `parseYandexReviews`).
  Проверено на всех 684 отзывах по 10 точкам — 0 коллизий.
- **Открытый риск, непроверяемый заранее**: всё вышеперечисленное
  проверено из настоящей браузерной сессии на рабочем компьютере. Будет
  ли антибот Яндекса так же спокоен к обычному `fetch()` с датацентровой
  стороны Vercel — узнаем только после первого реального прогона в
  проде. Если начнёт блокировать — переходить на VPS + браузерную сессию
  по схеме, описанной ниже для 1С/2ГИС-подобных случаев.
- Сопоставление код проекта ↔ orgId/orgSlug — захардкожено в
  `YANDEX_MAPS_LOCATIONS` внутри файла, резолвлено вручную из ссылок,
  присланных Антоном. У точки **ВС нет** страницы Яндекс Карт — её
  намеренно нет в списке.
- Ручной синк: `POST /api/v1/sync/yandex-maps` с `{projectCodes?}` — без
  кодов синкает все 10 точек сразу (в отличие от Такскома здесь нет
  диапазона дат — у Яндекса нет фильтра отзывов по дате, так что каждый
  синк перечитывает всё заново; это ок, это просто пачка обычных HTTP-
  запросов, а не по запросу на смену).
- Ночной автосинк: `GET /api/v1/sync/yandex-maps`, по расписанию из
  `vercel.json`, защищён тем же `CRON_SECRET`, что и Такском.
### Если Яндекс однажды закроет публичный доступ (2ГИС, Google Business — тоже сюда)

У 2ГИС и Google Business dashboard обычно нет публичного API для чтения
отзывов — единственный рабочий вариант тогда — забирать через
браузерную сессию. Рабочая схема (проверена на практике на похожих
проектах):

- Отдельный VPS (2vCPU/4GB достаточно) — Playwright/Selenium + cron,
  при необходимости VPN до сервиса.
- **Логин руками, локально, не с сервера.** Headless-браузер на VPS
  почти всегда палится антибот-системами (нет GPU → WebGL через
  software-эмуляцию, TLS-фингерпринт датацентра — обе вещи легко
  детектируются). Схема: логинишься руками в обычном браузере →
  экспортируешь сессию (cookies/localStorage, например
  `context.storage_state()` в Playwright) → заливаешь файл на сервер —
  дальше сервер использует готовую сессию, не логинится сам.
- Сессия протухает непредсказуемо (от часов до нескольких суток).
  Три меры вместе: (1) keepalive-крон на сервере раз в час — заходит
  с текущей сессией, если жива — пересохраняет; (2) по возможности
  автообновление сессии с рабочего компьютера (куки из обычного
  браузера через расписание); (3) уведомление о статусе сессии, а не
  узнавать о протухании только по факту упавшего ночного сбора.
- **Retry с бэкоффом на HTTP 429 — на каждом запросе**, не только на
  "главном" месте вызова. Похожий соседний метод запроса легко
  словит тот же rate-limit, если про него забыли.

## Общий принцип для всех коннекторов: стабильный ключ сущности

Для связи с записью в нашей БД используй **внутренний ID сущности из
источника** (id блюда/точки/заказа в кассе или агрегаторе), а не
производные вещи вроде названия или URL. Название блюда могут
переименовать в кассе, промо может временно менять отображаемое имя —
если ключом связи была строка, а не ID, история/аналитика по этой
сущности после переименования молча "обнулится" (система решит, что
это новая сущность). Закладывай ID-ключ с первого дня интеграции, а не
когда уже накопится история.

Каждый коннектор экспортирует одну async-функцию с чётким контрактом
входа/выхода — смотри комментарии в файле.
